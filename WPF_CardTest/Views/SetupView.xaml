<Window x:Class="WPF_CardTest.Views.SetupView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_CardTest.Views"
        mc:Ignorable="d"
        Title="SetupView" Height="200" Width="250">
    <Grid>
        <DockPanel>
            <Expander DockPanel.Dock="Top" Header="Communication" IsExpanded="True">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="30"/>
                        <!-- Added for the button -->
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Port"/>
                    <ComboBox Grid.Row="0" Grid.Column="2" ItemsSource="{Binding Ports}" SelectedItem="{Binding ComPort}"/>
                    <Button Grid.Row="2" Grid.Column="2" Content="Save" Command="{Binding SaveCommand}" Width="100" HorizontalAlignment="Left" Margin="0,5,0,0"/>
                </Grid>
            </Expander>
        </DockPanel>
    </Grid>
</Window>